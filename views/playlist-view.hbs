{{> menu}}

<section class="section">
  <div class="title">
    {{playlist.title}}
  </div>
  <div class="subtitle">
    Shortest Song: {{shortestTrack.title}}
  </div>
  {{> list-tracks}}
  {{> add-track}}
</section>